# BridgeAPT
BridgeAPT: Nucleic acid sequence generation from 3D structure using GVP + Transformer. Input a PDB file, generate candidate sequences, extract shape features via AptShape, and filter by clustering (K-Means, Hierarchical, GMM) with UMAP visualization.
